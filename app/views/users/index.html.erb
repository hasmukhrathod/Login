<%= will_paginate %>

<% @users.each do |user| %>
    <li>
       <%= link_to user.first_name, user %>
         <% if current_user.admin? && unless @user == current_user %>
	| <%= link_to "delete", user, method: :delete,data: { confirm: "You sure?" } %>
	<% end %>
	<% end %>

    </li>
  <% end %>
  
  <%= will_paginate %>